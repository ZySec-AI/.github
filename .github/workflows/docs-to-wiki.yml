name: docs-to-wiki

on:
  push:
    branches: [develop, stage, release]
    paths:
      - 'docs/**'
      - 'README.md'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    uses: ./.github/workflows/_reusable-docs-to-wiki.yml
    secrets: inherit
