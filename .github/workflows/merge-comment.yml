# This workflow has been consolidated into develop-pr-automation.yml
# Kept as a stub to avoid blocking the develop branch workflow parser.
# Safe to delete once develop is merged into main.
name: PR checklist comment (stub)

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is superseded by develop-pr-automation.yml"