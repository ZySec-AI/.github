name: License Compatibility Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: write
  contents: read

jobs:
  check-license:
    uses: ./.github/workflows/_reusable-check-license.yml
    secrets: inherit
