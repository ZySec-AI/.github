name: code review (reusable)

on:
  workflow_call:
    inputs:
      mode:
        type: string
        default: 'audit'
      fail_on:
        type: string
        default: 'CRITICAL'
      model:
        type: string
        default: 'deepseek/deepseek-v4-pro'
      base_url:
        type: string
        default: 'https://openrouter.ai/api'
    secrets:
      OPENROUTER_API_KEY:
        required: true
      ORG_BOOTSTRAP_TOKEN:
        required: true

jobs:
  agent:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo under review
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine context
        id: ctx
        run: |
          MODE="${{ inputs.mode }}"
          case "$MODE" in
            audit)
              BASE="${{ github.event.before }}"
              if [ -z "$BASE" ] || [[ "$BASE" =~ ^0+$ ]]; then
                BASE=$(git rev-parse HEAD~1 2>/dev/null || echo "${{ github.sha }}")
              fi
              echo "base=$BASE" >> $GITHUB_OUTPUT
              echo "head=${{ github.sha }}" >> $GITHUB_OUTPUT
              ;;
            pr-review)
              echo "base=${{ github.event.pull_request.base.sha }}" >> $GITHUB_OUTPUT
              echo "head=${{ github.event.pull_request.head.sha }}" >> $GITHUB_OUTPUT
              echo "pr_number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
              ;;
            issue-triage|issue-fix)
              echo "issue_number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
              echo "base=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
              echo "head=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
              ;;
          esac

      - name: Run code reviewer
        uses: ZySec-AI/code-reviewer@develop
        with:
          mode: ${{ inputs.mode }}
          zai-api-key: ${{ secrets.OPENROUTER_API_KEY }}
          zai-base-url: ${{ inputs.base_url }}
          model: ${{ inputs.model }}
          github-token: ${{ secrets.ORG_BOOTSTRAP_TOKEN }}
          base-sha: ${{ steps.ctx.outputs.base }}
          head-sha: ${{ steps.ctx.outputs.head }}
          pr-number: ${{ steps.ctx.outputs.pr_number }}
          issue-number: ${{ steps.ctx.outputs.issue_number }}
          fail-on: ${{ inputs.fail_on }}
