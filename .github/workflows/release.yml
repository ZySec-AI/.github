name: Release

on:
  push:
    branches: [release]
    tags: ['v*']
  pull_request:
    branches: [release]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

jobs:
  release:
    uses: ./.github/workflows/_reusable-release.yml
    secrets: inherit
    with:
      environment: production
      notify: true
