name: Sprint Retro Generator

on:
  schedule:
    - cron: '30 9 * * 5'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: read

jobs:
  retro:
    uses: ./.github/workflows/_reusable-sprint-retro.yml
    secrets: inherit
