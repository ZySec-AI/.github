name: Auto-Promote

on:
  pull_request:
    types: [closed]
    branches: [develop, stage]

permissions:
  pull-requests: write
  contents: read

jobs:
  promote:
    uses: ZySec-AI/.github/.github/workflows/_reusable-auto-promote.yml@develop
    secrets: inherit
